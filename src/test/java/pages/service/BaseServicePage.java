package pages.service;

import model.browser.RoleBrowser;
import pages.BasePage;

public class BaseServicePage extends BasePage {
    public BaseServicePage(RoleBrowser browser) {
        super(browser);
    }
}

